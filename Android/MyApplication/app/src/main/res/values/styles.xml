<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="myEditTextStyle">
        <item name="android:layout_width">@dimen/input_edit_width</item>
        <item name="android:layout_height">@dimen/input_edit_height</item>
        <item name="android:gravity">center</item>
        <item name="android:background">@drawable/my_edittext</item>
        <item name="android:textColorHint">#0650cf</item>
    </style>
</resources>
